# web/

Web library + creative renderer (TODO). Browses/plays recorded takes and renders
them with effects (particles, displacement, the lighting work from `crypt`).
Reuse the three.js renderer from the `crypt` repo. Mesh sequences delivered as
glTF + meshopt; vertex color now, texture-as-video later.
